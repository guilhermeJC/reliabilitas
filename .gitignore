# Documentos fundadores — SENSÍVEIS, local-only, nunca versionar (D28)
docs/*
!docs/README.md

# Posicionamento — documentação de brainstorming/apresentação do fundador,
# local-only, invisível (pedido do fundador, 11/07). Mesmo espírito do D28.
posicionamento/

# Journal/spec vivo de desenvolvimento — informal, pessoal, local-only desde
# a decisão de tornar o repo público (24/07/2026, mesmo espírito do D28).
# Continuam existindo no disco (sync entre ambientes fica fora do Git a
# partir de agora — mesmo padrão já usado por docs/ e posicionamento/).
CLAUDE.md

# Ferramental de revisão assistida — instrumento de trabalho do mantenedor,
# não parte do produto. Fica no disco (segue funcionando), fora do repo público.
.agents/
skills-lock.json
learning/

# Segredos — NUNCA versionar
.env
.env.*
!.env.example
config/deploy.env
*.pem
*.key

# Dependências e builds (M04)
node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo

# Playwright (G5)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Vercel / Supabase locais
.vercel/
.supabase/

# SO / editores
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Obsidian (se o vault enxergar esta pasta no futuro)
.obsidian/
.trash/

# Temporários
*.log
*.tmp
~$*
