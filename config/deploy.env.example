# Estrutura do config/deploy.env real (gitignored — nunca versionar).
# MVP: deploy via Vercel Git push — sem SSH/registry. Campos abaixo documentam o alvo.
VERCEL_PROJECT=reliabilitas
PRODUCTION_BRANCH=main
PRODUCTION_URL=https://reliabilitas.io
